{
    var nm = 'Android';
    var bl = true;
    var sal = 1000;
    //object//
    var obj = { key: 7552 }; //object which is having key and value
    var obj1 = { 1: 'shital' }; //we can assign this kind of objects which is restricted to its type like number and string in ts
    //functions//
    var fn = function () { return console.log('This is awsome'); }; //fn is hold the fun which does not hane any parameter which return void
    var fn1 = function (bl) {
        return 'boolean';
    };
    //let fn1:(b1:boolean)=> string (b1:boolean) => 'true'//another way to write same function inline
    //this fun is a type of bollean which returning a string
    //mixed type of data
    var mixed = true; ///its a function which can support any type of variable boolean , numbe, string,function void
    //universal//
    var uni = function () { return ''; }; //hold any type of data
}
{
    // function calculate (mob:string): void | boolean {
    // }
    //calculate : '1567421'
    //calculate:'true'//
}
